package main

func main() {
	// filePath := "./i18n/errors_pt.json"

	// validationErrors, err := validation.LoadValidationErrors(filePath)
	// if err != nil {
	// 	log.Fatalf("Error to load error files: %v", err)
	// }
}
